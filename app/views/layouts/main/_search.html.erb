<%= form_tag(snippets_path, { class: 'navbar-form snippets-search-form', method: :get, remote: true }) do |f| %>
  <div class="input-group margin-bottom-sm">
    <span class="input-group-addon">
      <i class="fa fa-search fa-fw"></i>
    </span>
    <%= text_field_tag :query, params[:query], class: 'form-control typeahead snippets-search', placeholder: 'Search' %>
  </div>
<% end %>
